export enum ModuleName {
  AUTH = "auth",
  TEMPLATE = "template",
}

export enum PageName {
  LOGIN = "login",
  TEMPLATE = "list",
  TEMPLATE_FORM = "form",
}

export enum PagePath {
  HOME = "/",
  LOGIN = "/login",
  TEMPLATE = "/template",
  TEMPLATE_FORM = "/template/form",
}
